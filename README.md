# MAQ-Attaq
A python script to automate an Active Directory attack path levearging MAQ.

# Setup
  1. Setup a python virtual environment
  ```
  python3 -m venv venv && source venv/bin/activate
  ```
